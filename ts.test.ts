// const sum = (a: number, b: number): number => {
//   return a + b;
// };

// test("타입스크립트 함수 테스트하기", () => {
//   expect(sum("1", 2)).toBe(3);
// type/jest 설치해서 이걸 바로 쓸 수 있는 것이다.
// });

// "1" 문자열로 바꾸면 타입스크립트에서 이미 빨간줄로 표시해줌.
// 테스팅적 관점에서 결코 좋지는 않다.
//   프론트 레벨에서도 TS적 처리가 잘 되어있으면, 프론트에서나 테스팅 레벨에서나 문제가 될 가능성이 없다.
// 버그가 날 가능성 자체를 없앤다. 그래서 타입스크립트가 각광받고 있는 것임.

// --------------------------------------------------------------
